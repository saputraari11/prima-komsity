"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
const serializer = {
    parse(text) {
        const value = JSON.parse(text);
        const { cookie, lastModified } = value, rest = __rest(value, ["cookie", "lastModified"]);
        return Object.assign({ cookie: Object.assign(Object.assign({}, cookie), { expires: cookie.expires === undefined ? undefined : new Date(cookie.expires) }), lastModified: lastModified === undefined ? undefined : new Date(lastModified) }, rest);
    },
    stringify(value) {
        var _a;
        const { cookie, lastModified } = value, rest = __rest(value, ["cookie", "lastModified"]);
        const data = Object.assign({ cookie: Object.assign(Object.assign({}, cookie), { expires: (_a = cookie.expires) === null || _a === void 0 ? void 0 : _a.getTime() }), lastModified: lastModified === null || lastModified === void 0 ? void 0 : lastModified.getTime() }, rest);
        return JSON.stringify(data);
    },
};
exports.default = serializer;
//# sourceMappingURL=serializer.js.map